# X 博主帖子自动采集方案

> 版本：v1.0  
> 整理日期：2026-09-17  
> 目标：用尽量不增加费用的方式，定期收集指定 X 博主的公开帖子，保存为 SQLite 数据及 Markdown 文件。  
> 交付性质：实施方案与开发任务书，不是已经完成的采集程序，也不代表定时任务已经部署。

---

## 1. 方案结论

采用以下路线：

```text
配置目标博主
    ↓
Python 单次运行脚本
    ↓
FxTwitter：JSON 接口优先，RSS 作为兼容模式
    ↓
解析、校验、去重、记录覆盖情况
    ↓
SQLite 保存帖子及运行状态
    ↓
导出每篇帖子 Markdown + 每日新增索引
    ↑
Windows 任务计划程序每小时触发一次
```

**先验证采集源，再开发最小闭环；不先做网页后台，也不先引入 Agent。**

JSON 和 RSS 都属于此前建议的 FxTwitter 路线。JSON 优先是为了方便保留结构化字段和分页，RSS 用于接口兼容性验证及有限降级，不是另起一套技术方案。

这里的“免费”是实施约束：第一版不购买 API、不订阅采集服务、不调用收费大模型，使用已有电脑或设备运行。现有网络、电费、维护时间仍是成本；第三方公开服务的可用性和未来收费规则不能保证。

### 1.1 必须先知道的限制

FxEmbed 当前文档列出了用户时间线 JSON 接口和账号 RSS。[1][2] **本次整理时，运行环境对两个接口域名的请求都遇到了 DNS 解析失败，没有取得有效帖子返回。**因此，本文确认的是文档能力，不是已经完成接口实测，更不是长期稳定性背书。

开发必须把“阶段 0：采集源验证”作为上线前置条件。接口不通时，只能继续做离线测试和不依赖上游的模块，不能把模拟数据当成真实采集成功。

---

## 2. 第一版范围与默认参数

以下是建议初始值，不是用户已经确认的账号清单，也不是服务方承诺的安全频率。

| 项目 | 第一版约定 |
|---|---|
| 目标账号 | 先用 3 个公开账号验证，真实清单通过配置文件填写 |
| 轮询频率 | 默认每 60 分钟一次，可改为每 120 分钟 |
| 请求方式 | 串行处理，不并发轰击接口 |
| 每页数量 | 默认 50 条 |
| 正常轮询页数 | 每账号每轮最多 3 页，达到覆盖条件可提前结束 |
| 首次导入 | 尝试获取近 72 小时，每账号最多 3 页，不承诺导入完整历史 |
| 重叠窗口 | 默认回看最近一个已确认扫描边界之前的 6 小时 |
| 内容范围 | 原创、引用帖、博主本人发出的回复；不抓别人对该博主的全部评论 |
| 转帖 | 保存能识别的记录，默认不进入每日阅读索引 |
| 图片、视频 | 保存上游返回的链接与描述，不批量下载媒体文件 |
| 正文语言 | 保留原文，不自动翻译 |
| 保存方式 | SQLite 为唯一事实来源，Markdown 为可重新生成的阅读输出 |
| 运行环境 | 优先在 Windows 本地验证；稳定后可迁移到常开 Mac 或 NAS |
| 展示时区 | 默认跟随运行机器本地时区；数据库统一保存 UTC |

第一版不做：历史全量抓取、实时秒级监听、登录绕过、私密账号采集、账号池、代理池、自动回复或发布、自动转载、完整评论树、AI 摘要、Web 管理后台。

**验收目标是“持续收集、避免重复、发现并暴露缺口”，不是“保证一条不漏”。**

## 3. 技术选型与项目结构

### 3.1 技术栈

| 层次 | 建议选型 | 用途 |
|---|---|---|
| 运行环境 | Python 3.12 | 作为项目开发基线，不要求追逐最新版本 |
| 命令行 | 标准库 `argparse` | 配置检查、单次采集、导出、查看状态 |
| 配置 | TOML | 使用 `config.toml` 管理账号与参数 |
| 网络 | `httpx` | GET 请求、超时、代理环境变量 |
| 数据库 | Python `sqlite3` | 去重、状态、运行记录；无需另起数据库服务 [4] |
| RSS 兼容 | `feedparser` | 仅在启用 RSS 适配器时引入 |
| HTML 清理 | 小型 HTML 解析模块 | RSS 正文转纯文本；不得直接执行 HTML |
| 单实例锁 | `filelock` | 避免手动运行和定时运行同时写入 |
| 测试 | `pytest` | 使用离线样本验证解析、去重及故障处理 |
| 定时触发 | Windows 任务计划程序 | 触发单次命令，程序完成后退出 |

依赖在开发验收时锁定具体版本，提交锁定文件。不在本方案中猜测最新依赖版本。

不引入 FastAPI、Redis、消息队列、向量库或多 Agent 编排。第一版是小型命令行项目，不是通用爬虫平台。

### 3.2 建议目录

Windows 项目目录建议为 `D:\side\x-watch`，实际位置可自行调整。

```text
x-watch/
├── README.md
├── PLAN.md
├── pyproject.toml
├── requirements.lock.txt
├── config.example.toml
├── config.toml                   # 实际账号配置，不提交
├── .gitignore
├── src/x_watch/
│   ├── __main__.py               # 命令行入口
│   ├── config.py
│   ├── collector.py              # 单轮采集与覆盖检查
│   ├── providers/
│   │   ├── fxtwitter_json.py
│   │   └── fxtwitter_rss.py       # 兼容模式，按阶段实现
│   ├── normalize.py
│   ├── storage.py
│   └── export.py
├── scripts/
│   ├── run_once.ps1
│   └── register_task.ps1
├── tests/
│   └── fixtures/                # 人工构造或脱敏样本
├── docs/
│   └── source-validation.md     # 真实验证记录
├── data/
│   ├── x-watch.sqlite3
│   └── raw/                     # 限期保留的接口响应
├── output/
│   ├── posts/                   # 每篇一个 Markdown
│   └── daily/                   # 按发现日期生成索引
└── logs/
```

不要为了目录齐全而预先堆积空模块。先实现 JSON 单账号闭环，再按职责拆分。

## 4. 阶段 0：采集源验证

### 4.1 验证什么

分别验证 JSON 和 RSS，但先只对一个公开账号发少量请求。

```text
JSON：
https://api.fxtwitter.com/2/profile/{handle}/statuses?count=5&with_replies=1

RSS：
https://fxtwitter.com/{handle}/feed.xml?count=5&with_replies=1
```

以上路径及参数来自当前 FxEmbed 文档。[1][2] `{handle}` 填用户名，不带 `@`。

可先在 PowerShell 中使用下面的探测命令。`bcherny` 仅为沿用上一轮讨论的示例账号，可替换为实际目标。

```powershell
# 此处只探测接口，不创建长期任务，也不要求 X 登录凭证。
curl.exe -i -sS --connect-timeout 10 --max-time 30 `
  "https://api.fxtwitter.com/2/profile/bcherny/statuses?count=5&with_replies=1"

curl.exe -i -sS --connect-timeout 10 --max-time 30 `
  "https://fxtwitter.com/bcherny/feed.xml?count=5&with_replies=1"
```

### 4.2 必须记录的证据

在 `docs/source-validation.md` 中记录测试时间、运行环境、请求参数、HTTP 状态、内容类型、返回数量、最新帖时间、分页结果以及人工核对结论。

核查重点：

- 返回的是 JSON/XML，而不是登录页、验证码页、错误 HTML 或假成功页面。
- 账号、帖子 ID、发布时间和正文能与可访问的原帖对应，纯媒体帖允许正文为空。
- JSON 的下一页确实带来不同结果；不能只看到一个 `cursor` 就宣布分页可用。
- 检查回复、引用帖、长文及转帖样本；暂时找不到的类型明确标为“未验证”。
- 选取几条近期可见帖子核对是否出现；这种抽查只能验证样本，不能证明全量覆盖。

接口文档给出了 `results`、`cursor` 和错误码等结构，但最终适配器必须以真实样本和文档交叉验证，不凭经验捏造字段。[1][3]

### 4.3 通过与阻断条件

| 验证结果 | 后续处理 |
|---|---|
| JSON 可用，基础字段及分页可验证 | 进入 JSON 主流程开发 |
| JSON 可用，但分页未能验证 | 可以实现最近一页收集；标记为有限覆盖，不承诺补漏 |
| JSON 不可用、RSS 可用 | 使用 RSS 有限模式；暂不实现假分页 |
| 两者均不可用 | 记录阻断原因，继续离线模块；暂停真实定时采集上线 |
| 只返回验证码、登录页或非公开内容错误 | 停止该请求，不开发绕过机制 |

**DNS 失败只能说明当前环境未连通，不能直接断言服务已关闭；HTTP 200 也不能单独证明采集成功。**

## 5. 数据源适配规则

### 5.1 JSON 主模式

一次完整请求处理为：检查 HTTP 状态 → 检查内容类型 → 解析 JSON → 检查业务错误码与字段 → 标准化数据。

文档约定通过 `cursor.bottom` 取得下一页，并作为下次请求的 `cursor` 参数。[3] 游标按不透明字符串处理，不解码、不自行拼装，不假设永久有效。

第一版不启用线程聚合和自动翻译，减少返回结构变化；需要回复时使用文档提供的参数。

**不要把 `since` 当成数据库查询中的严格增量过滤器。**文档描述的 204 行为与首个返回页面内是否存在更新内容有关，不能据此证明更深页面无遗漏。[1] 第一版默认不传 `since`；补漏也不能依赖 204 判断完成。

### 5.2 RSS 兼容模式

RSS 用来验证最小收集链路，或在已验证可用时切换为有限模式。

从条目永久链接或 GUID 中解析帖子 ID，保留正文、原帖链接、发布时间及上游提供的媒体信息。RSS 的 description 是 HTML，需要安全提取文本。[2]

RSS 适配器必须声明 `supports_pagination = false`。第一版不假设它可以翻页，不把设置较大的 `count` 当成完整历史能力。

RSS 与 JSON 应按共享 FxTwitter 上游依赖处理，而不是两家独立供应商。因此：

- 不把“换成 RSS”描述为高可用保障。
- 429、403、验证码或认证限制出现时，不通过切换格式继续冲击同一服务。
- 第一版默认不自动切换，先验证、记录能力差异，再由配置明确选择。

### 5.3 统一内部结果

建议适配器返回本项目定义的结构，业务层不直接依赖第三方完整 JSON：

```text
FetchPage
  source               数据源与格式
  fetched_at           UTC 获取时间
  posts[]              标准化帖子
  next_cursor          下一页游标，可能为空
  supports_pagination  是否支持已验证的分页
  warnings[]           不完整、未知类型、字段缺失等提示
  raw_response_ref     原始响应定位信息
```

未知字段保留在原始响应中。未知帖子类型不得直接丢弃，应标记为 `unknown`，防止误判导致漏收。

## 6. 采集、去重与补漏流程

### 6.1 总体原则

**每轮都从最新一页开始，先保存拿到的有效数据，再判断是否需要向历史分页。**

不能只保存最大帖子 ID，然后丢弃所有更小的 ID。这样会错过晚返回内容、旧帖转发以及故障后补回的旧帖。

不能“遇见一条旧帖或已存在的帖子就停止”。置顶帖、引用内容、转帖中的原帖时间都可能使这个判断失真。

### 6.2 首次导入

首次启动时，按近 72 小时、最多 3 页的预算尝试导入，以先到达者为准。这个窗口是抓取目标，不是完整性保证。

导入记录标记为 `bootstrap`。它们可以生成归档文件，但不能全部当成“刚刚发布的新帖”推送。

如果预算不足以覆盖 72 小时，保存已取得的数据，并明确记录“首次导入窗口未完全覆盖”。不要为了补历史而阻塞后续新帖收集。

### 6.3 每轮扫描

```text
取得单实例锁
读取配置并验证参数

对每个启用账号：
    检查是否处于退避或暂停状态
    记录本轮开始时间，并保存本轮之前的已知帖子集合
    从最新一页请求，页数预算默认为 3

    对每页：
        检查响应、业务错误和字段
        对有效条目标准化，并以事务方式入库
        更新账号与帖子之间的关联
        统计新增、已知、更新、未知类型与解析错误
        评估重叠窗口是否已扫描，以及是否仍存在可疑缺口
        如果满足经过验证的停止条件则结束
        如果有有效且未访问过的游标，则请求下一页
        否则结束，并记录覆盖结论

    记录本轮成功、有限覆盖、部分失败或失败
    仅在符合条件时推进扫描边界

从数据库重新生成受影响的 Markdown 和日期索引
记录本轮摘要，释放锁并退出
```

### 6.4 停止分页与覆盖结论

把运行状态和覆盖判断分开。接口请求成功，不等于完整性得到证明。

建议覆盖状态如下：

| 覆盖状态 | 含义 |
|---|---|
| `bootstrap` | 初次有限导入，之前的历史不在承诺范围内 |
| `overlap_observed` | 扫描已回到此前已知区间，且覆盖了配置的重叠目标；不是全量保证 |
| `source_exhausted` | 上游在已验证的分页结构中明确返回末页；仅代表该源返回结束 |
| `limited` | RSS、预算不足、游标异常、次序不可判断等导致无法确认覆盖 |
| `unknown` | 失败或证据不足，不能判断覆盖情况 |

实现停止条件时要注意：

1. 重叠判断使用**本轮开始前**已存在的记录，不能把刚入库的条目当成历史锚点。
2. 只有经过样本验证的正常时间线条目才能参与时间边界判断。置顶、转帖、引用原帖和未知类型不得单独触发停止。
3. 在可验证的顺序下，扫描应达到“上次已确认边界前 6 小时”，而不是仅遇到旧记录就结束。
4. 无法确认顺序或时间语义时，按预算继续取页，最终标记有限覆盖。宁可多给一个警告，也不声称完整。
5. 到达页数上限、出现重复游标或下一页失败时，保留已入库数据，记录风险，不假装已补齐。

### 6.5 边界与缺口分开保存

账号状态至少区分：

- `last_attempt_at`：最近尝试时间，包括失败。
- `last_valid_response_at`：最近取得合法响应的时间，不表示覆盖完整。
- `last_scan_boundary_at`：在本采集策略下最后一次完成覆盖检查的扫描起点时间。
- `gap_from_at` / `gap_to_at`：尚未解决的疑似缺口时间范围，可为空。

扫描边界应采用成功一轮的**开始时间**，不能用最大帖子发布时间，也不能用任务结束时间代替。

存在缺口时，之后某轮只收到了最新一页，不得自动清除旧缺口。后续每轮仍先抓最新内容，避免历史问题阻断新帖收集。

第一版不做无人值守无限历史回补。超出正常预算的缺口保留状态，提供有明确时间范围和页数上限的手动 `backfill` 命令；游标失效时重新从可用入口开始，不能无限重试。

## 7. 数据模型

采用 SQLite，第一版使用四张核心表即可。下面是本项目的数据设计，不是对第三方完整字段的照抄。

### 7.1 `posts`：帖子内容

| 字段 | 用途 |
|---|---|
| `post_id TEXT PRIMARY KEY` | 帖子 ID，始终作为字符串处理 |
| `author_id` / `author_handle` | 实际作者，不等于监控账号 |
| `url` | 原帖链接 |
| `text` | 原文；纯媒体帖子允许为空字符串 |
| `published_at` | 原帖发布时间，UTC；无法解析时为空并告警 |
| `post_type` | `original / reply / quote / repost / unknown` |
| `reply_to_id` / `quote_post_id` | 上游明确返回时保存，否则为空 |
| `media_json` | 媒体链接、类型、描述，缺失不伪造 |
| `metrics_json` | 上游返回的互动数量快照，缺失使用 null 而不是 0 |
| `content_hash` | 正文及稳定内容字段的哈希，用于识别内容变化 |
| `first_seen_at` / `last_seen_at` | 本系统首次与最近发现时间 |
| `content_updated_at` | 最近一次观察到正文或内容字段变化的时间 |
| `source` / `raw_json` | 来源及当前选中的有效原始内容 |
| `content_status` | `normal / suspected_truncated / unavailable / unknown` |

相同帖子再次出现时更新 `last_seen_at` 及有证据的变化，但不重复插入。缺字段或降级 RSS 不得把已有完整正文覆盖为空；互动数字变化也不应造成正文归档反复更新。

帖子编辑可能在不同来源表现为同 ID 内容变化或新 ID。第一版只做实际观察记录；无法确认关联时不得靠文本相似度强行合并。

### 7.2 `account_posts`：监控账号与帖子关联

建议唯一键为 `(monitored_handle, post_id)`，保存关联类型及首次、最近观察时间。

这张表解决“监控账号 A 的时间线里出现作者 B 的帖子”的问题。`posts` 中保存作者 B，不得把作者改成 A。

同一原帖被多个监控账号转发时，帖子正文只保存一份，账号关联分别保存。第一版只保证内容级去重，不承诺记录同一账号对同一帖子多次转发的每个动作。

仅凭“作者与监控账号不同”不足以确认转帖；没有明确证据时，关联类型记为 `unknown`。

### 7.3 `account_state`：账号运行状态

保存最近尝试、合法响应、扫描边界、疑似缺口、连续失败次数、下次允许请求时间、覆盖状态，以及 `scope_hash`。

`scope_hash` 由账号、是否包含回复等影响采集范围的配置生成。改变范围或切换数据源时，旧边界不能直接视为新范围已经扫描完成，需要一次新的覆盖验证。

### 7.4 `fetch_runs`：运行记录

每轮每账号记录开始、结束、来源、尝试请求数、页数、返回数、新增数、更新数、有效条目数、覆盖状态及错误类型。

新增量通过本轮实际插入结果统计，不以返回条数代替。保存成功的数据与失败原因都要可追溯。

数据库写入使用参数化 SQL、短事务及显式提交；参数化操作和事务控制可参考 Python 官方文档。[4]

## 8. 配置文件示例

下列账号仅用于说明配置格式，不代表用户已经确定监控它们。正式运行前修改账号清单。

```toml
[app]
database_path = "data/x-watch.sqlite3"
output_dir = "output"
raw_dir = "data/raw"
log_dir = "logs"
display_timezone = "local"

[source]
provider = "fxtwitter_json"
auto_fallback = false
page_size = 50
include_replies = true

[collection]
# 该值供任务注册脚本读取；run-once 本身不常驻调度。
interval_minutes = 60
max_pages_per_account = 3
bootstrap_hours = 72
bootstrap_max_pages = 3
overlap_hours = 6
request_gap_seconds = 5
connect_timeout_seconds = 10
read_timeout_seconds = 30
max_attempts_per_request = 2
max_total_requests_per_run = 30
max_run_seconds = 600

[export]
# 内容尽量先保存；过滤只决定阅读索引是否展示。
include_reposts = false
include_unknown = true
write_post_files = true
write_daily_index = true

[retention]
raw_days = 7
log_days = 30

[[accounts]]
handle = "bcherny"
enabled = true

[[accounts]]
handle = "simonw"
enabled = true

[[accounts]]
handle = "dotey"
enabled = true
```

配置加载必须验证账号格式、重复账号、参数范围、页数和预算，并把用户名规范化。相对路径统一相对于配置文件目录解析，不能受当前工作目录影响。

`max_total_requests_per_run` 要把分页和重试都算进去。达到总预算时，未执行账号标记为 `deferred`；下轮优先处理上次被延后的账号，防止列表尾部长期饥饿。

**代码中的默认值、示例配置、README 和计划任务间隔必须一致。**修改 `interval_minutes` 后需要更新系统任务，不能只改配置就假装触发频率改变。

## 9. Markdown 输出

### 9.1 每篇帖子独立归档

建议路径：

```text
output/posts/{post_id}.md
```

文件名只使用已验证的帖子 ID，避免用户名变化或不可信文本造成路径问题。

每篇文件包含作者、原帖链接、发布时间、发现时间、监控来源、内容类型、正文及媒体链接。正文保留原文，转帖和引用要标清主体。

不把帖子文字拼接成可执行命令，不直接输出未经处理的 HTML。链接只接受安全的 HTTP/HTTPS 协议；转义 Markdown 结构，防止正文破坏文件格式。

### 9.2 每日新增索引

建议路径：

```text
output/daily/2026-09-17.md
```

日期按**首次发现时间**及配置的展示时区计算，排序以首次发现时间为主。补到一周前的帖子也必须出现在今天的新增记录中，并明确显示原始发布时间。

每个日期索引包含：运行概况、首次发现的帖子、初次导入标识、内容更新及未解决缺口。索引只链接归档文件，不重复堆放所有长正文。

不要简单向 Markdown 追加文本。应从数据库按日期重新渲染，以避免脚本重跑导致重复。采用临时文件加原子替换；导出失败不回滚已经成功入库的帖子，之后可单独重建。

### 9.3 第一版不自动调用 AI

第一版只负责“拿到、存好、能查、能追溯”。之后增加摘要、标签或翻译时，保留原文并单独存储生成结果。

收费 API 或其他付费流程不能默认开启。外部帖子即便包含“执行命令”“忽略规则”等文字，也只能作为待分析数据，不能成为 Agent 的操作指令。

## 10. 错误处理与服务保护

| 情况 | 必须行为 |
|---|---|
| DNS 失败、连接失败、超时 | 记录为网络问题；有限退避重试，不修改扫描边界 |
| HTTP 5xx | 允许有限重试；仍失败则结束该账号本轮 |
| HTTP 429 | 尊重 `Retry-After`，支持秒数或 HTTP 日期；暂停同源剩余请求并保存下次可请求时间 |
| 无 `Retry-After` 的 429 | 建议初始退避 2 小时，连续出现再加长；这是本项目保护策略 |
| HTTP 401/403、验证码、登录页 | 停止并明确报告，不绕过，不换格式规避限制 |
| 404 或账号不可访问 | 记录“当前不可访问”，不武断判断是删号、改名还是上游错误 |
| HTTP 200 但 JSON `code` 错误 | 按失败处理，不记作空列表 |
| 200 但返回 HTML 或结构不兼容 | 标记解析/结构错误，保存有限诊断样本 |
| 合法空列表 | 记录“源返回空”；不能据此证明账号没有发帖或历史已覆盖 |
| 部分条目无法解析 | 保存有效条目，记录失败数量，本轮状态为 partial，不静默推进边界 |
| 重复游标、循环分页 | 立即停止当前分页，标记 limited |
| 数据库写入失败 | 回滚当前事务，记录严重错误，不推进相关状态 |
| 导出失败 | 保留采集结果，记录导出错误，支持独立重建 |
| 已有进程持有锁 | 本轮不重复执行，明确记录 skipped |

普通临时错误可先等待约 30 秒再进行一次重试，但总请求数及运行时长仍受配置限制。长期退避写入数据库，不让进程挂起等待数小时。

一次 429 可能影响同源所有账号，不应只跳过当前账号后继续请求其余账号。第一版通过统一的来源退避状态或一致更新账号状态实现，不引入分布式限流服务。

连续失败告警建议阈值为 3 次。第一版告警体现在日志和每日索引中，不等于已经配置微信、邮件或手机通知。

## 11. 命令行与运行方式

### 11.1 开发完成后应提供的命令

下面定义的是待实现的命令协议，不是当前已经存在的软件。

```powershell
# 检查配置、数据库路径、网络和少量接口返回
python -m x_watch --config config.toml doctor

# 单次采集：定时任务只调用这一条
python -m x_watch --config config.toml run-once

# 只采集一个账号，便于排查
python -m x_watch --config config.toml run-once --handle bcherny

# 查看账号状态、最近运行和未解决缺口
python -m x_watch --config config.toml status

# 只从数据库重建某日索引，不请求上游
python -m x_watch --config config.toml export --date 2026-09-17

# 有界补漏：时间必须使用带时区的 ISO 8601，且设置页数上限
python -m x_watch --config config.toml backfill --handle bcherny `
  --from 2026-09-15T00:00:00Z --to 2026-09-17T00:00:00Z --max-pages 10
```

`backfill` 同样遵守来源退避、间隔和请求预算。它不是保证能取回该时间段的承诺；源不支持历史分页时应明确拒绝该能力，而不是默默只抓最新一页。

建议进程退出码：`0` 为本轮按策略正常完成，`1` 为 partial/failed 或导出失败，`2` 为配置或初始化错误。覆盖范围仍应单独展示，退出码 0 不能被解释为无遗漏。

### 11.2 Windows 本地优先

开发完成后，项目应支持以下安装路径：

```powershell
Set-Location D:\side\x-watch
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.lock.txt
.\.venv\Scripts\python.exe -m pip install -e . --no-deps
Copy-Item config.example.toml config.toml
# 编辑 config.toml，填入真实账号后再运行。
.\.venv\Scripts\python.exe -m x_watch --config config.toml doctor
.\.venv\Scripts\python.exe -m x_watch --config config.toml run-once
```

`pyproject.toml` 与锁定依赖由开发阶段生成，版本和依赖范围保持一致。

计划任务通过 `scripts/run_once.ps1` 调用绝对路径虚拟环境解释器，明确工作目录，保留程序退出码。不能依赖手动激活虚拟环境或某个终端里设置过的变量。

Windows 支持按小时创建计划任务，可参考 Microsoft 官方命令文档。[6] 注册脚本应支持创建、检查与删除，并在注册前展示任务名称、执行命令、运行身份和间隔。

任务设置需要处理：上次任务尚未结束时不启动新实例、错过触发后的补执行策略、执行身份对目录的写权限、重启后运行及日志路径。

电脑休眠、关机或断网期间不能正常抓取。因此，本地验证通过后，再决定是否迁移到常开设备。本文没有替用户注册任何系统任务。

### 11.3 网络与凭证

HTTPX 可以读取 `HTTP_PROXY`、`HTTPS_PROXY` 等环境变量。[5] 应通过受控环境配置网络，不在代码中硬编码代理地址，更不假设浏览器可访问就代表计划任务可访问。

在计划任务实际使用的账号和环境下再运行一次 `doctor`。检查终端临时变量、系统变量与任务环境是否一致。

FxTwitter 主路线不要求本项目保存 X 密码、Cookie 或官方 API Key。确需启用其他来源时另行评估；不要把凭证发给陌生服务或提交到 Git。

## 12. 运维与成本控制

正常情况下，每个账号每小时取 1 页，3 个账号每天约 72 次请求；每轮都达到 3 页则约 216 次，均未计入重试和补漏。这只是由本方案参数计算出的请求量，不是供应商免费额度或稳定性承诺。

上线前至少确认下面几件事：

- `status` 能看到每个账号最近尝试、合法响应、扫描边界和疑似缺口。
- 原始响应默认保留 7 天，日志默认 30 天；数据库和 Markdown 不自动删。
- 原始数据、代理信息、实际配置、日志不提交 Git；错误日志避免泄露认证信息。
- 数据库放本机磁盘；不要让多台机器同时写同一份文件，也不要在运行中直接云同步活跃数据库。
- 备份通过 SQLite 备份接口，或在进程完全停止后进行；定期试一次恢复及 Markdown 重建。

扩大账号数之前，先看页数、失败率和覆盖状态。不要把“接口暂时能用”当成可以无限扩容的理由。

## 13. 分阶段开发与验收清单

每一阶段按“实现 → 测试 → 核对验收 → 记录结果”推进。通过后再进入下一阶段；外部采集源阻断时保留事实，不编造通过记录。

### 阶段 0：真实来源验证

- [ ] 选择一个公开账号，分别验证 JSON 和 RSS。
- [ ] 保存真实响应的脱敏样本及验证记录。
- [ ] 确认基础字段、正文、链接、时间及分页行为。
- [ ] 记录未知能力和当前网络限制。

**验收：能够取得并核对至少一条真实公开帖子，或者明确记录阻断原因。只有前者才能进入真实采集上线流程。**

### 阶段 1：单账号最小闭环

- [ ] 建立项目、配置加载和 `doctor`。
- [ ] 实现一个 JSON 适配器及单次采集。
- [ ] 建立数据库及单实例锁。
- [ ] 生成单篇 Markdown。
- [ ] 重复运行验证去重和正文更新。

**验收：同一批有效数据连续运行两次，不新增重复帖子；能从文件追溯原帖；失败与空结果明确区分。**

### 阶段 2：多账号与可靠性

- [ ] 增加多账号串行处理、总请求预算和轮转顺序。
- [ ] 实现账号关联、首次导入标识和范围变更检测。
- [ ] 实现分页、重叠扫描、缺口记录和有界补漏。
- [ ] 完成超时、429、错误页、重复游标等故障测试。
- [ ] 根据阶段 0 的证据决定是否实现 RSS 有限模式。

**验收：部分失败不丢失已保存数据，也不把未完成扫描标成完整；转帖不归错作者；故障恢复不产生重复。**

### 阶段 3：阅读输出与定时任务

- [ ] 实现每日首次发现索引和独立重建命令。
- [ ] 实现状态查看、日志轮转和原始响应清理。
- [ ] 编写 Windows 启动与任务注册脚本。
- [ ] 在实际任务身份下验证网络、路径、写权限和退出码。
- [ ] 验证重复触发、重启和错过任务后的行为。

**验收：可以在任务实际环境中自动完成一轮，SQLite 与 Markdown 一致，重复触发不会产生两个写进程。**

### 阶段 4：真实观察与发布

- [ ] 以 3 个账号运行观察 3～7 天，保留真实运行记录。
- [ ] 抽查这段时间内可访问的帖子与本地归档，记录发现的差异。
- [ ] 根据账号活跃度调整页数或频率，不直接假设覆盖完整。
- [ ] 验证备份恢复、停用账号、修改回复范围及来源变化。
- [ ] 完成 README、依赖锁定、测试结果和已知限制。

**验收：持续运行情况有证据，缺口与故障可见，没有未经说明的付费依赖。观察期是上线验收要求，不能用几次模拟运行声称已完成。**

## 14. 必须覆盖的测试场景

| 场景 | 预期结果 |
|---|---|
| 同一帖子在同一页或不同页重复出现 | `posts` 只保留一条 |
| 同一帖子由两个监控账号带出 | 正文一条、账号关联两条 |
| 同 ID 正文更新 | 更新内容，不增加一条新帖 |
| RSS 降级缺少原有字段 | 不把已有完整内容覆盖为空 |
| 纯媒体帖子没有文字 | 作为有效帖子保存，不误报解析失败 |
| 第一条是很旧的置顶帖 | 不因此停止分页 |
| 一条未知类型或时间线顺序异常 | 保留记录并标记不确定，不静默丢弃 |
| 返回的条数少于 page_size，但仍有游标 | 不仅凭条数判定历史结束 |
| 第一页成功、第二页失败 | 保留第一页，标记 partial，保留缺口 |
| 429 带 Retry-After | 本轮暂停同源请求，重启后仍遵守退避 |
| HTTP 200 返回错误 HTML | 记录失败，不记作“没有新帖” |
| 合法空列表或暂不可访问账号 | 保存明确状态，不推断删号或无更新 |
| 多个条目只有部分字段不合规 | 有效条目入库，失败数量可追溯 |
| 重复游标或分页循环 | 有限停止，不无限请求 |
| 故障后拿到早于最大 ID 的漏帖 | 仍能入库，并在发现当日索引出现 |
| 达到页数或总请求预算 | 标记 limited/deferred，后续账号不会一直被饿死 |
| 采集中进程中断、导出失败或任务重入 | 已提交数据不丢失，不重复输出，状态可恢复 |
| 切换数据源或启用回复 | 重新验证范围，不能沿用旧边界声称全覆盖 |
| 正文包含 HTML、命令或恶意 Markdown | 安全保存为数据，不执行内容 |

## 15. 免费主路线失效后的处理

不要把备选平台一次性都开发出来。第一版只把数据源模块与业务层隔开，保留替换空间。

优先顺序是：定位网络或解析问题 → 明确当前能力下降 → 保留数据和缺口 → 再评估其他来源。

RSSHub 自建或带免费额度的采集服务可作为后续评估方向，但本版本不把它们当作已验证可用、无限免费或无需维护的保障。

任何需要新凭证、账户授权、付费或改变数据用途的切换，都必须单独记录并确认。不能静默从免费模式切到收费模式。

只处理获准访问的公开内容，尊重适用的平台与来源使用条件。保留作者及原帖链接；公开转载或商业再分发不属于本采集方案的默认授权范围。

## 16. 交给 Codex 的开发指令

下面这段可以与本文件一起作为项目开发输入：

```text
请按《X 博主帖子自动采集方案》开发 x-watch。

目标是个人使用的低成本指定账号采集工具。第一版采用 Python、FxTwitter、SQLite、Markdown 和系统计划任务，不做 Web 后台、多 Agent、收费 API 或账号池。

先核对现有项目目录，不覆盖已有内容。先完成阶段 0 的真实采集源验证，保存验证记录；无法联网或接口不通时，如实记录阻断，只继续离线模块与测试，不伪造响应或宣布端到端通过。

按阶段逐项实现、测试和验收。当前阶段通过后继续下一阶段，不因为普通可解决问题反复要求确认。需要新增付费、敏感凭证、外部发送或变更数据权限时停止相应操作并说明。

重点落实：ID 去重、作者与监控账号区分、从最新页开始、重叠扫描、有限分页、疑似缺口记录、失败不推进边界、429 同源退避、单实例锁、数据库先于 Markdown、补回旧帖也进入发现当日索引。

只输出有证据的测试结果。实现模拟测试不等于真实接口验证，短时运行不等于完成 3～7 天观察。README 中清楚区分已实现、已实测和受上游限制的能力。

最终交付可安装运行的源码、依赖锁定、示例配置、Windows 脚本、测试、验证记录、README 与已知限制。不得承诺绝对不漏帖或免费服务永久可用。
```

---

## 17. 资料来源与核验范围

以下资料核对日期为 2026-09-17。引用用于支持接口和工具能力；本文的频率、预算、数据模型、边界策略、阶段划分与测试标准是本项目设计建议，不是来源方承诺。

[1] FxEmbed：List user statuses。接口路径、参数、状态码及字段说明。  
https://docs.fxembed.com/api/twitter/operations/2profilehandlestatuses/

[2] FxEmbed：RSS & Atom feeds。账号 feed 地址、查询参数和 RSS 内容结构。  
https://docs.fxembed.com/guide/advanced/rss-atom-feeds/

[3] FxEmbed：API Overview。API 版本、业务响应及游标分页说明。  
https://docs.fxembed.com/api/introduction/

[4] Python 官方：sqlite3。数据库连接、参数化 SQL、事务与备份。  
https://docs.python.org/3/library/sqlite3.html

[5] HTTPX 官方：Environment Variables。HTTP 代理及环境配置。  
https://www.python-httpx.org/environment_variables/

[6] Microsoft Learn：schtasks create。Windows 计划任务创建及调度参数。  
https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/schtasks-create

**实测说明：本次成功读取了上述相关文档，但对 FxTwitter JSON 和 RSS 的实际请求均因当前运行环境 DNS 解析失败而未取得数据。应在最终部署机器上重新验证。**
